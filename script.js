const logoutMenu = document.getElementById('logout-menu')
const userInitials = document.getElementById('user-initials-container')

function toggleLogoutMenu(){
    logoutMenu.classList.toggle('d-none')
}